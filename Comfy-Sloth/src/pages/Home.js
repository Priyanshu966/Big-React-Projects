import styled from "styled-components";
import { Hero, Services, Contact } from "../components";

const Home = () => {
  return (
    <main>
      <Hero />
      <Services />
      <Contact />
    </main>
  );
};

export default Home;
